sudo: false
language: ruby
cache: bundler
rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.0
  - 2.2.0
  - 2.3.0
script:
  - bundle exec rake
  - bundle exec rake build format_json && git status -s && [[ -z $(git status -s) ]]
  - for f in `find . -name '*.json'`; do diff -q $f <(jq "." $f); done
